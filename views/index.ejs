<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>WhatsApp Loja Bot</title>
  <style>
    body {
      text-align: center;
      font-family: Arial, sans-serif;
      background: #f5f5f5;
    }
    img {
      margin-top: 40px;
      border: 5px solid #00e676;
      border-radius: 10px;
    }
  </style>
</head>
<body>
  <h1>📱 Escaneie o QR Code</h1>
  <div id="qrcode"><p>Aguardando conexão...</p></div>
  <script src="/socket.io/socket.io.js"></script>
  <script>
    const socket = io()
    socket.on('qr', (qrCode) => {
      document.getElementById('qrcode').innerHTML = `<img src="${qrCode}" width="300">`
    })
    socket.on('ready', (msg) => {
      document.getElementById('qrcode').innerHTML = `<h2>${msg}</h2>`
    })
  </script>
</body>
</html>
